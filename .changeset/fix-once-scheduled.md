---
"budgie": patch
---

Fix scheduled transactions with "Once" frequency: they now show a "Once" badge (was "Daily") and are removed from the list once recorded, auto-posted, or skipped, since they will never occur again.
